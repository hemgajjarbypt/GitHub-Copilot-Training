{
  "name": "week4-utils",
  "version": "1.0.0",
  "description": "Utility functions for string parsing, API fetching, and math calculations",
  "main": "main.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
